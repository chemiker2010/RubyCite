# 💎 RubyCite v1.0 使用说明书

本手册旨在指导您在 macOS 环境下完成 RubyCite v1.0 的配置、数据导入、AI 降维分析、综述生成以及与 Zotero / EndNote 的闭环联动。

---

## 🛠️ 第一步：底座配置 (Settings)

在正式处理文献前，必须先建立底座算力连接。RubyCite 坚持**本地优先**，但同时提供主流的云端弹性分流方案。

### 1. 启动并连接 LLM
进入系统左侧导航栏的 **Settings** 标签页，根据您的算力资产选择以下一种模式：
* **模式 A：RubyCite built-in (内置引擎)**
    * **操作**：将您离线下载好的标准 `.gguf` 格式大模型文件，物理放入应用的 `models/` 文件夹内。
    * **适用**：追求 100% 单机物理断网、对隐私保密有极致要求的独立运行场景。
* **模式 B：Ollama 服务网关**
    * **操作**：在终端启动您本地的 Ollama 服务（确保执行 `ollama serve`）。在软件中填入本地网关地址（默认 `http://localhost:11434`），并在模型列表中选择您已下载的模型（如 `qwen2.5-coder:32b` 或云端映射指针 `qwen3-coder-next:cloud`）。
* **模式 C：云端 API 模式**
    * **操作**：选择 OpenAI、DeepSeek 或其他兼容格式的第三方提供商，填写对应的 API Base URL 与 Secret Key。

### 2. 连通性测试与环境持久化
* 选择好模型后，务必点击 **测试连接**，确保网络或本地 IPC 通道握手成功。
* 在界面中设置 **界面/综述语言**（可选中文或英文）。
* （可选）开启 **Crossref 元数据自动补全** 开关。当识别到有效 DOI 时，系统会在后台自动通过学术接口对齐论文的高级元数据。
* 完成所有配置后，点击 **保存设置**。

---

## 📄 第二步：文献管道化解析 (Convert)

将杂乱的、具有排版噪音的 PDF 转换为结构化的可编辑资产。

### 1. 导入 PDF 文献
* 进入 **Convert** 标签页，点击 **Select PDFs…** 按钮。
* 选择一篇或多篇**英文 SCI 期刊原版 PDF**（注意：必须是文字可选中、可复制的矢量 PDF，纯扫描件效果有限）。

### 2. 管道化数据输出解析
解析完成后，在左侧列表中点击对应文献。此时主工作区会将其解构并持久化存储在本地磁盘中。
右侧面板会展示以下提取成果，供您检查或手动微调：
* **Overview (元数据窗)**：自动识别出的标题、作者、期刊、年份及 DOI（若不准，可在此处手动修改，或点击 Crossref 刷新）。
* **主工作区**：完全脱水、抹平了单双栏排版噪音的标准 Markdown 正文。
* **右侧图表预览**：系统从 PDF 物理图层中自动剥离并导出的独立矢量插图与表格文件。

> 💾 **物理存储结构**
> 每次成功转换后，本地数据目录将自动生成以下文件结构：
> * `markdown/{论文名}.md`：完整的论文正文 Markdown，头部包含标准的 YAML 元数据区。
> * `markdown_assets/{论文名}/`：该篇论文内部所有被提取出来的独立图片与图表资产。
> * `pdfs/`：原始 PDF 文件的本地归档副本。

---

## 📊 第三步：单篇文献要素降维 (Analyze)

在阅读长篇大论前，利用 AI 将文献迅速压缩为分子级要素卡片。

### 1. 触发 AI 摘要卡片
* 切换到 **Analyze** 标签页，在左侧文献列表中选择您刚刚转换好的 **完整版 `.md` 文件**（注意：不要勾选带有 `.digest.md` 后缀的文件）。
* 点击 **Generate cards** 按钮。

### 2. 要素卡片审阅与个人笔记
系统将消耗极少量的 Token，为该论文建立 3 张高密度的专属摘要卡片：
* **Card 1：核心摘要 (Executive Summary)**
* **Card 2：所攻坚的科学问题与实验方法标签 (Problems & Methodologies)**
* **Card 3：研究的材料或物理边界系统 (Material/Boundary Systems)**

在主工作区右下角的 **My notes** 文本框中，您可以随时记录您在阅读时的即时想法或组会评语，完成后点击 **Save notes**。

> 💾 **物理存储结构**
> 系统会在 `markdown/` 目录下创建一个伴生的独立文件 `{论文名}.digest.md`。该文件仅包含 YAML 元数据、AI 生成的 3 张要素卡片以及您的个人阅读笔记，完全剥离了原刊的长篇正文，极度轻量化，专为后续的综述投喂而准备。

---

## 🕸️ 第四步：多篇文献无幻觉综述 (Review)

基于您的私有文献资产，由 AI 产出具备严格引文定位的综述草稿。

### 1. 框定知识源边界（按场景选择投喂策略）
进入 **Review** 标签页，根据您的写作体量，勾选左侧文献树中的对应文件：
* **策略 A：投喂全文（Full .md）**
    * **规则**：直接勾选不带后缀的论文全文。为了防止长文本导致的大模型注意力分散或上下文溢出，**建议全文投喂数量严格控制在 20 篇以内**。
    * **适用**：小篇幅的阶段性进展汇报、小综述（Mini-Review）或特定技术细节的深度对比。
* **策略 B：投喂要素卡片（.digest.md）**
    * **规则**：勾选经过 Analyze 降维后生成的轻量化要素卡片。
    * **适用**：**撰写学位论文（毕业论文）或重大项目报告等需要引用上百篇文献的宏大场景**。投喂卡片能极大压缩输入体积，避免爆掉 LLM 上下文，同时确保 AI 能够精准横向对比海量文献的底层逻辑。

选中完成后，点击 **Feed to model**，将选中的资产锁定为本次大模型的唯一局部上下文。

### 2. 撰写结构化提示词
* 在中间的 **Review prompt** 输入框中，写下您具体的综述要求。
    * *示例提示词：请帮我梳理上述文献中关于挥发性有机物（VOCs）转化的技术演进。请重点对比催化燃烧法与蓄热式热氧化（RTO）在空速控制和能耗上的边界系统差异。*

### 3. 生成带引文锚点的草稿
* 点击 **Generate review**。
* **防幻觉机制**：AI 将被死死限制在您勾选的文献范围内进行逻辑重构，绝不从外网捏造任何不存在的文献。
* 生成的综述文本内部，会自动在关联论点后方带上标准的 **`{作者, 年份}`** 临时引文锚点（例如：`...而高空速下催化剂容易发生热失活 {Smith, 2023}。`）。您可以直接点击复制或保存。

---

## 💾 第五步：对接 Zotero / EndNote 传统生态 (Export RIS)

实现从 AI 综述文本向 Word 论文正式排版的无缝闭环转换。

### 1. 导出参考文献标准库
* 在 **Review** 标签页的侧边栏或底部，点击 **Export RIS…** 按钮。
* 系统会扫描您本次勾选的所有文献，根据其内部存储的完整元数据，一键导出为一个标准的 `library.ris` 参考文献库文件（即使您勾选的是 digest 文件，系统也会自动关联其全文元数据）。

### 2. 文献管理器对接与 Word 刷新
1. 打开您日常使用的传统文献管理软件（如 **Zotero** 或 **EndNote**），将刚刚生成的 `.ris` 文件直接拖拽导入。
2. 将 RubyCite 生成的综述正文粘贴到您的 **Microsoft Word** 或 **WPS** 论文草稿中。
3. 确保您的 Zotero/EndNote 插件已在 Word 中启用，点击文献工具栏上的 **Update Citations（更新引文）** 或 **Bibliography Refresh**。
4. 传统文献软件会自动捕捉文本中的 `{Smith, 2023}` 临时锚点，并与刚刚导入的 RIS 数据库匹配，一键将其全部转换为您目标期刊要求的标准上标（如 `[1]`）并在文末自动生成完美的参考文献列表。

---

## 📂 本地数据物理存储指南 (Data Path)

为了方便工程师和习惯使用局域网同步工具（如 Syncthing）的用户进行增量备份，RubyCite 所有的应用行为和生成资产全部暴露在标准的本地文件系统中。

在 macOS 生产环境下，您可以通过点击侧栏的 **Open data folder** 直接唤醒 Finder，其物理存储绝对路径为：
`~/Library/Application Support/RubyCite/`

### 目录清单与运维对应关系：
* `/markdown/`：转换后的 Full 文献与伴生的 Digest 要素卡片（Markdown 格式）。
* `/markdown_assets/`：从各篇 PDF 中剥离出来的图片、静态表格与插图文件。
* `/pdfs/`：您导入的原始 PDF 备份库。
* `/models/`：供内置 llama.cpp 引擎读取的离线 `.gguf` 大模型存放点。
* `library.ris`：应用内自动维护的参考文献索引库。
* `settings.json`：您配置的 LLM 密钥、本地网关地址与语言习惯配置文件。

---

## ❓ 常见问题排查 (FAQ)

* **Q: PDF 导入后转换失败或 Markdown 出现大面积乱码？**
    * **A**: 请确保 PDF 是出版社出版的原装正版电子期刊（即文本可以被鼠标正常选中复制）。由于本工具侧重于高精度学术管道重构，网页直接另存为的 PDF、教研室手工扫描件或带有大量加粗水印的盗版文献，转化效果会受到较大限制。
* **Q: 触发 Analyze 或 Review 时系统提示 LLM 处于离线状态？**
    * **A**: 请回到 Settings 页面检查您的提供商配置。如果是使用 Ollama 模式，请确保您在终端执行过 `ollama run` 且服务未被防火墙拦截；如果是内置模式，请检查 `models/` 文件夹下模型文件的后缀名是否为标准的 `.gguf`。
* **Q: 系统是否支持处理中文 PDF 文献？**
    * **A**: 当前 v1.0 版本的信息抽取流水线（包括 Crossref 元数据自动对齐）是针对 **英文 SCI 期刊 PDF** 深度定制的，目前对中文期刊 PDF 的支持非常有限。
